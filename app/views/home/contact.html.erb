<div class="contact-page">

  <section class="contact-hero">
    <div class="hero-glow"></div>

    <div class="contact-badge">
      <span class="badge-icon">✉</span>
      <span>CONTACT</span>
    </div>

    <h1>ご意見・ご要望</h1>

    <div class="hero-line"></div>

    <p class="hero-sub">
      より良い国試対策アプリを目指して
    </p>
  </section>


  <main class="contact-main">

    <div class="contact-heading">
      <span class="heading-line"></span>
      <h2>お気軽にお送りください</h2>
      <span class="heading-line"></span>
    </div>

    <p class="contact-description">
      追加してほしい問題・改善点<br>
      バグ報告などをお待ちしています
    </p>


    <div class="contact-card">

      <div class="card-label">
        <span class="label-icon">✎</span>
        <span>MESSAGE</span>
      </div>

      <textarea
        class="feedback-box"
        placeholder="ご意見・ご要望をご入力ください"
      ></textarea>

      <div class="contact-note">
        皆さまの声をアプリ改善の参考にさせていただきます
      </div>

    </div>


    <button class="send-btn" disabled>
      <span>送信</span>
      <small>準備中</small>
    </button>

    <p class="coming-soon">
      COMING SOON
    </p>

  </main>

</div>


<style>
* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background:
    radial-gradient(circle at 50% 25%, rgba(39, 104, 190, 0.06), transparent 34%),
    linear-gradient(180deg, #f8faff 0%, #f3f7fd 100%);
  color: #0b2345;
}

.contact-page {
  width: 100%;
  max-width: 680px;
  min-height: 100vh;
  margin: 0 auto;
  padding-bottom: 40px;
}


/* =========================
   上部ヒーロー
========================= */

.contact-hero {
  position: relative;
  overflow: hidden;
  text-align: center;
  padding: 38px 20px 34px;
  background:
    linear-gradient(
      120deg,
      #061d3d 0%,
      #0b3768 48%,
      #3975a7 100%
    );
  border-radius: 0 0 34px 34px;
  box-shadow: 0 10px 28px rgba(12, 45, 86, 0.18);
}

.hero-glow {
  position: absolute;
  width: 240px;
  height: 240px;
  right: -70px;
  top: -80px;
  border: 1px solid rgba(255, 255, 255, 0.07);
  border-radius: 50%;
}

.hero-glow::before,
.hero-glow::after {
  content: "";
  position: absolute;
  border: 1px solid rgba(255, 255, 255, 0.05);
  border-radius: 50%;
}

.hero-glow::before {
  width: 170px;
  height: 170px;
  top: 34px;
  left: 34px;
}

.hero-glow::after {
  width: 100px;
  height: 100px;
  top: 69px;
  left: 69px;
}

.contact-badge {
  position: relative;
  z-index: 2;
  display: inline-flex;
  align-items: center;
  gap: 9px;
  margin-bottom: 14px;
  padding: 6px 15px;
  border: 1px solid rgba(231, 199, 112, 0.65);
  border-radius: 999px;
  color: #e9cf83;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 2px;
}

.badge-icon {
  font-size: 15px;
}

.contact-hero h1 {
  position: relative;
  z-index: 2;
  margin: 0;
  color: #ffffff;
  font-size: clamp(29px, 7vw, 42px);
  font-family: serif;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-shadow: 0 2px 10px rgba(0, 0, 0, 0.16);
}

.hero-line {
  position: relative;
  z-index: 2;
  width: 54px;
  height: 2px;
  margin: 15px auto 12px;
  background: linear-gradient(
    90deg,
    transparent,
    #e7c770,
    transparent
  );
}

.hero-sub {
  position: relative;
  z-index: 2;
  margin: 0;
  color: rgba(255, 255, 255, 0.78);
  font-size: 12px;
  letter-spacing: 0.08em;
}


/* =========================
   メイン
========================= */

.contact-main {
  padding: 28px 20px 0;
}

.contact-heading {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 12px;
}

.contact-heading h2 {
  margin: 0;
  color: #102b50;
  font-family: serif;
  font-size: clamp(20px, 5vw, 27px);
  font-weight: 700;
  white-space: nowrap;
  letter-spacing: 0.04em;
}

.heading-line {
  width: 42px;
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent,
    #c8a94e
  );
}

.heading-line:last-child {
  background: linear-gradient(
    90deg,
    #c8a94e,
    transparent
  );
}

.contact-description {
  margin: 12px 0 20px;
  text-align: center;
  color: #607089;
  font-size: 14px;
  font-weight: 600;
  line-height: 1.8;
}


/* =========================
   入力カード
========================= */

.contact-card {
  padding: 18px;
  background: rgba(255, 255, 255, 0.94);
  border: 1px solid #dbe5f2;
  border-radius: 22px;
  box-shadow:
    0 10px 30px rgba(25, 58, 100, 0.08),
    inset 0 1px 0 rgba(255, 255, 255, 0.9);
}

.card-label {
  display: flex;
  align-items: center;
  gap: 7px;
  margin: 0 0 10px 4px;
  color: #b49745;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1.7px;
}

.label-icon {
  font-size: 14px;
}

.feedback-box {
  display: block;
  width: 100%;
  height: 180px;
  padding: 16px;
  resize: none;
  outline: none;
  border: 1.5px solid #d5dfeb;
  border-radius: 15px;
  background: #fbfcff;
  color: #152d4d;
  font-family: inherit;
  font-size: 15px;
  line-height: 1.7;
  transition: 0.2s ease;
}

.feedback-box::placeholder {
  color: #aab3bf;
}

.feedback-box:focus {
  border-color: #5c8dc4;
  background: #ffffff;
  box-shadow: 0 0 0 4px rgba(54, 116, 179, 0.09);
}

.contact-note {
  margin-top: 12px;
  text-align: center;
  color: #99a5b4;
  font-size: 10px;
  line-height: 1.5;
}


/* =========================
   送信ボタン
========================= */

.send-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 9px;
  width: min(78%, 330px);
  margin: 22px auto 0;
  padding: 13px 24px;
  border: none;
  border-radius: 999px;
  background: linear-gradient(
    110deg,
    #174e91,
    #4877bc
  );
  color: #ffffff;
  font-family: inherit;
  font-size: 16px;
  box-shadow: 0 8px 20px rgba(23, 78, 145, 0.18);
  opacity: 0.55;
}

.send-btn small {
  padding-left: 9px;
  border-left: 1px solid rgba(255, 255, 255, 0.4);
  font-size: 11px;
  font-weight: 500;
}

.coming-soon {
  margin: 9px 0 0;
  text-align: center;
  color: #aab4c1;
  font-size: 8px;
  font-weight: 700;
  letter-spacing: 2px;
}


/* =========================
   スマホ調整
========================= */

@media (max-width: 480px) {

  .contact-hero {
    padding: 31px 16px 29px;
    border-radius: 0 0 28px 28px;
  }

  .contact-main {
    padding: 24px 17px 0;
  }

  .contact-heading {
    gap: 8px;
  }

  .heading-line {
    width: 28px;
  }

  .contact-description {
    font-size: 13px;
    margin-bottom: 17px;
  }

  .contact-card {
    padding: 14px;
    border-radius: 19px;
  }

  .feedback-box {
    height: 165px;
    font-size: 14px;
  }

  .send-btn {
    margin-top: 19px;
  }
}
</style>