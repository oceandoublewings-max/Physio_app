<style>
  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    background: #eef3f9;
    color: #102847;
  }

  .about-page {
    width: 100%;
    max-width: 820px;
    margin: 0 auto;
    padding: 12px;
  }

  .about-hero {
    position: relative;
    overflow: hidden;
    padding: 18px 18px 17px;
    border-radius: 18px;
    color: #fff;
    background:
      linear-gradient(
        120deg,
        rgba(5, 31, 68, 0.99) 0%,
        rgba(8, 56, 110, 0.96) 100%
      );
    box-shadow: 0 10px 24px rgba(13, 43, 83, 0.18);
  }

  .about-hero::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      linear-gradient(
        135deg,
        transparent 58%,
        rgba(255, 255, 255, 0.08) 58.5%,
        transparent 59%
      );
  }

  .about-hero-inner {
    position: relative;
    z-index: 1;
  }

  .about-brand {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
  }

  .about-brand-icon {
    font-size: 22px;
    line-height: 1;
  }

  .about-brand-title {
    margin: 0;
    color: #fff;
    font-family: serif;
    font-size: clamp(22px, 6vw, 32px);
    font-weight: 700;
    letter-spacing: 0.02em;
    line-height: 1.15;
    white-space: nowrap;
  }

  .about-hero-line {
    width: 92px;
    height: 1px;
    margin: 11px auto 9px;
    background: linear-gradient(
      90deg,
      transparent,
      #d9b85c,
      transparent
    );
  }

  .about-brand-subtitle {
    margin: 0;
    text-align: center;
    color: #e8eef7;
    font-size: clamp(12px, 3.5vw, 15px);
    line-height: 1.45;
  }

  .about-section {
    margin-top: 12px;
    padding: 17px 16px;
    border: 1px solid #e0e8f2;
    border-radius: 16px;
    background: #fff;
    box-shadow: 0 5px 15px rgba(20, 52, 89, 0.08);
  }

  .about-section-heading {
    display: flex;
    align-items: center;
    gap: 9px;
    margin: 0 0 12px;
    color: #15375e;
  }

  .about-section-mark {
    width: 4px;
    height: 29px;
    flex-shrink: 0;
    border-radius: 10px;
    background: linear-gradient(#2d78dd, #174eaa);
  }

  .about-section-icon {
    font-size: 20px;
    line-height: 1;
  }

  .about-section-title {
    margin: 0;
    font-family: serif;
    font-size: clamp(21px, 5vw, 27px);
    line-height: 1.2;
  }

  .about-copy {
    margin: 0;
    color: #39495d;
    font-size: 15px;
    line-height: 1.72;
  }

  .about-copy + .about-copy {
    margin-top: 10px;
  }

  .about-copy strong {
    color: #143b6b;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 8px;
  }

  .feature-card {
    min-width: 0;
    padding: 12px 10px;
    border: 1px solid #dfe9f7;
    border-radius: 12px;
    background: linear-gradient(145deg, #ffffff, #f7faff);
    box-shadow: 0 3px 9px rgba(29, 68, 112, 0.06);
  }

  .feature-top {
    display: flex;
    align-items: center;
    gap: 7px;
  }

  .feature-icon {
    width: 31px;
    height: 31px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 9px;
    background: linear-gradient(145deg, #eaf3ff, #d9e9ff);
    font-size: 17px;
  }

  .feature-title {
    margin: 0;
    color: #132d50;
    font-size: clamp(14px, 3.8vw, 17px);
    font-weight: 700;
    line-height: 1.3;
    white-space: nowrap;
  }

  .feature-text {
    margin: 7px 0 0 38px;
    color: #5c6878;
    font-size: 12px;
    line-height: 1.45;
  }

  .about-message {
    display: grid;
    grid-template-columns: 44px 1fr;
    gap: 11px;
    align-items: center;
    margin-top: 12px;
    padding: 15px 16px;
    border: 1px solid #d6e6f7;
    border-radius: 16px;
    background: linear-gradient(135deg, #edf6ff, #ffffff);
    box-shadow: 0 5px 14px rgba(30, 77, 128, 0.07);
  }

  .about-message-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 1px solid #aac9ee;
    border-radius: 50%;
    background: #fff;
    font-size: 22px;
  }

  .about-message-title {
    margin: 0;
    color: #143b6b;
    font-family: serif;
    font-size: clamp(18px, 4.8vw, 23px);
    line-height: 1.3;
  }

  .about-message-text {
    margin: 5px 0 0;
    color: #4d5d70;
    font-size: 13px;
    line-height: 1.55;
  }

  .about-back {
    margin-top: 12px;
    text-align: center;
  }

  .about-back a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    min-width: 160px;
    padding: 10px 16px;
    border-radius: 11px;
    color: #fff;
    text-decoration: none;
    background: linear-gradient(135deg, #174f9c, #236fd0);
    box-shadow: 0 4px 10px rgba(29, 86, 160, 0.2);
    font-size: 14px;
    font-weight: 700;
  }

  @media (max-width: 380px) {
    .about-page {
      padding: 9px;
    }

    .about-hero {
      padding: 16px 12px 15px;
    }

    .about-section {
      padding: 15px 13px;
    }

    .feature-grid {
      gap: 7px;
    }

    .feature-card {
      padding: 11px 8px;
    }

    .feature-icon {
      width: 28px;
      height: 28px;
      font-size: 15px;
    }

    .feature-text {
      margin-left: 35px;
      font-size: 11px;
    }
  }
</style>

<div class="about-page">

  <section class="about-hero">
    <div class="about-hero-inner">
      <div class="about-brand">
        <span class="about-brand-icon">📘</span>
        <h1 class="about-brand-title">PT・OT共通基礎学習</h1>
      </div>

      <div class="about-hero-line"></div>

      <p class="about-brand-subtitle">
        PT・OT学生のための学習サポートアプリ
      </p>
    </div>
  </section>

  <section class="about-section">
    <div class="about-section-heading">
      <span class="about-section-mark"></span>
      <span class="about-section-icon">📖</span>
      <h2 class="about-section-title">About</h2>
    </div>

    <p class="about-copy">
      <strong>PT・OT共通基礎学習</strong>は、
      PT・OT学生の基礎学習をサポートするアプリです。
    </p>

    <p class="about-copy">
      解剖学・生理学・運動学を中心に、
      基礎を繰り返し学びながら、
      授業・定期試験・国家試験対策まで幅広く活用できます。
    </p>

    <p class="about-copy">
      毎日少しずつでも続けられる学習体験を大切にし、
      基礎を着実に身につけられるよう設計しています。
    </p>
  </section>

  <section class="about-section">
    <div class="about-section-heading">
      <span class="about-section-mark"></span>
      <span class="about-section-icon">✨</span>
      <h2 class="about-section-title">主な機能</h2>
    </div>

    <div class="feature-grid">

      <div class="feature-card">
        <div class="feature-top">
          <div class="feature-icon">📚</div>
          <h3 class="feature-title">5択問題</h3>
        </div>
        <p class="feature-text">重要ポイントを効率よく学習。</p>
      </div>

      <div class="feature-card">
        <div class="feature-top">
          <div class="feature-icon">⭕</div>
          <h3 class="feature-title">○×問題</h3>
        </div>
        <p class="feature-text">知識を素早くチェック。</p>
      </div>

      <div class="feature-card">
        <div class="feature-top">
          <div class="feature-icon">🖼️</div>
          <h3 class="feature-title">イラスト問題</h3>
        </div>
        <p class="feature-text">図で理解を深める。</p>
      </div>

      <div class="feature-card">
        <div class="feature-top">
          <div class="feature-icon">📊</div>
          <h3 class="feature-title">苦手分析</h3>
        </div>
        <p class="feature-text">苦手を分析して効率よく復習。</p>
      </div>

      <div class="feature-card">
        <div class="feature-top">
          <div class="feature-icon">🏅</div>
          <h3 class="feature-title">学習スタンプ</h3>
        </div>
        <p class="feature-text">スタンプを集めて楽しく継続。</p>
      </div>

      <div class="feature-card">
        <div class="feature-top">
          <div class="feature-icon">📄</div>
          <h3 class="feature-title">基礎学習資料</h3>
        </div>
        <p class="feature-text">イラスト付きPDFで学習。</p>
      </div>

    </div>
  </section>

  <section class="about-message">
    <div class="about-message-icon">🌱</div>

    <div>
      <h2 class="about-message-title">
        学び続ける力を育てる。
      </h2>

      <p class="about-message-text">
        今後も問題・イラスト・資料を継続的に追加し、
        より良い学習環境を提供できるよう改善を続けていきます。
      </p>
    </div>
  </section>

  <div class="about-back">
    <a href="/">← ホームへ戻る</a>
  </div>

</div>
